services:
  flask_app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: .
        target: /app
      - type: bind
        source: /media/devmon/digital_media
        target: /media/devmon/digital_media
    restart:
      unless-stopped
