services:
  flask_app:
    build: .
    user: "1000:1000"
    ports:
      - "5000:5000"
    environment:
      - XDG_CACHE_HOME=/tmp/.cache
    volumes:
      - type: bind
        source: /mnt/ceph-videos/YouTube
        target: /mnt/ceph-videos/YouTube
    restart:
      unless-stopped
