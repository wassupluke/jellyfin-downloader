services:
  flask_app:
    build: .
    user: "1000:1000"
    ports:
      - "5000:5000"
    volumes:
      - type: bind
        source: /mnt/ceph-videos
        target: /mnt/ceph-videos
    restart:
      unless-stopped
